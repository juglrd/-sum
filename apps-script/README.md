# Google Apps Script backend

Create an Apps Script project, paste the Code.gs from this folder, add `ADMIN_PASSWORD` under Project Settings > Script Properties, and deploy the script as a Web app. Then put the deployed `/exec` URL into `API_URL` in `index.html`.
